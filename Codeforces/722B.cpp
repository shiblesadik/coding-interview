#include <bits/stdc++.h>
using namespace std;

int n, m, k, p[101];
char arr[101][101];

int main (void) {
  scanf("%d", &n);
  for (int i = 0; i < n; ++i) scanf("%d ", &p[i]);
  for (int i = 0; i < n; ++i) gets(arr[i]);

  return 0;
}
